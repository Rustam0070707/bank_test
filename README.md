# 🚀 Система Управления Банковскими Картами

Backend-приложение на основе Spring Boot для управления банковскими картами с безопасной аутентификацией, контролем доступа на основе ролей и операциями с картами, такими как переводы, блокировка и просмотр баланса.

## 📌 Обзор

Этот проект реализует Систему Управления Банковскими Картами, которая позволяет администраторам управлять пользователями и картами, а пользователям — безопасно управлять своими картами. Система следует принципам чистой архитектуры и использует современные технологии Spring.

## 🏗 Структура Проекта

Проект имеет многоуровневую и хорошо организованную структуру:

```
src/main/java/com/example/bankcards/
├── config          # Конфигурация приложения (например, SecurityConfig)
├── controller      # REST-контроллеры
├── dto             # Объекты для передачи данных (Data Transfer Objects)
├── entity          # Сущности JPA
├── enums           # Перечисления (например, CardStatus, Role)
├── exception       # Пользовательские исключения и глобальный обработчик
├── Mapper          # Мапперы MapStruct
├── repository      # Репозитории JPA
├── security        # Конфигурация Spring Security и JWT
├── service         # Бизнес-логика
└── util            # Вспомогательные классы
```

Временные файлы README, использовавшиеся во время разработки, были удалены из финальной сборки.

## 🔐 Аутентификация и Авторизация

- Реализовано с использованием Spring Security + JWT
- Контроль доступа на основе ролей:
  - `ADMIN`
  - `USER`
- Безопасная аутентификация на основе токенов для всех защищенных эндпоинтов

## 💳 Функциональность Карт

Каждая карта содержит следующие атрибуты:
- **Номер карты** (зашифрован и маскирован как `**** **** **** 1234`)
- **Владелец**
- **Срок действия**
- **Статус**:
      - `ACTIVE`
      - `BLOCKED`
      - `EXPIRED`
      - `BLOCK_REQUESTED`
- **Баланс**

### Возможности Пользователя

- Просмотр собственных карт (с поиском и пагинацией)
- Просмотр баланса карты
- Запрос на блокировку карты
- Перевод средств между собственными картами

### Возможности Администратора

- Создание, активация, блокировка и удаление карт
- Управление пользователями
- Просмотр всех карт в системе

## 🔁 Возможности API

- CRUD-операции для карт
- Эндпоинты для управления пользователями
- Переводы между собственными картами пользователя
- Поддержка фильтрации и пагинации
- Валидация ввода с информативными сообщениями об ошибках

## 🔒 Безопасность

- Чувствительные данные карт зашифрованы
- Номера карт маскируются в ответах API
- Контроль доступа на основе ролей применяется на уровне эндпоинтов
- Stateless-аутентификация на основе JWT

## 🗄 База Данных и Миграции

- Варианты баз данных (в настоящее время настроено для MySQL):
  - PostgreSQL (требуется добавление зависимости для драйвера)
  - MySQL
- **ORM**: Spring Data JPA
- Управление схемой через **Liquibase**
- Скрипты миграции находятся по адресу:
  ```
  src/main/resources/db/migration
  ```
- Включает создание:
  - Таблицы пользователей (Users)
  - Таблицы карт (Cards)
  - Таблицы переводов (Transfers)

## 📄 Документация API

- Доступна документация OpenAPI / Swagger
- Файл спецификации:
  ```
  docs/openapi.yaml
  ```
- Может использоваться со Swagger UI или другими инструментами OpenAPI

## 🐳 Развертывание

- Включены конфигурации Docker и Docker Compose
- Позволяет легко настроить среду разработки
- Сервисы:
  - Сервис приложения
  - Сервис базы данных
- Запуск с помощью Docker Compose:
  ```bash
  docker-compose up --build
  ```

## 🧪 Тестирование

- Реализованы модульные тесты для ключевой бизнес-логики
- Покрытые сервисы:
  - `CardService`
  - `UserService`
- Обеспечивает корректность основных операций, таких как переводы, смена статуса карты и управление пользователями

## ⚠ Обработка Ошибок

- Пользовательские классы исключений для ошибок, специфичных для домена
- Глобальный обработчик исключений обеспечивает:
  - Единообразные ответы об ошибках
  - Четкие сообщения об ошибках валидации и бизнес-логики

## 🛠 Используемые Технологии

- **Java 17+**
- **Spring Boot**
- **Spring Security**
- **Spring Data JPA**
- **JWT**
- MySQL (поддержка PostgreSQL доступна при смене драйвера)
- **Liquibase**
- **Docker & Docker Compose**
- **Swagger / OpenAPI**
